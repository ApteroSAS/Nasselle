// Define the provider type properly for clarity
export type Provider = {
    provider_name: string;
    provider_url: string;
    ram: string;
    storage: string;
    cpu: string;
    price: string;
};